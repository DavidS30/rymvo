"use client";
import { useEffect, useState } from "react";
type Driver = { id: string; fullName: string; email: string; phone: string | null; isAvailable: boolean; assignedCount: number };
export default function AdminDriversPage() { const [drivers, setDrivers] = useState<Driver[]>([]); useEffect(() => { fetch("/api/v1/admin/drivers").then(r => r.json()).then(d => setDrivers(d.data ?? [])); }, []); return <div className="p-4 sm:p-8"><p className="rymvo-eyebrow">Operación</p><h1 className="mt-2 text-2xl font-bold">Conductores</h1><p className="mt-1 text-sm text-gray-500">Disponibilidad y carga de viajes asignados.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{drivers.map(d => <article key={d.id} className="rymvo-card rounded-xl p-5"><div className="flex items-start justify-between"><div><h2 className="font-semibold">{d.fullName}</h2><p className="mt-1 text-sm text-gray-500">{d.email}</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${d.isAvailable ? "bg-green-100 text-green-700" : "bg-gray-100 text-gray-500"}`}>{d.isAvailable ? "Disponible" : "No disponible"}</span></div><div className="mt-5 border-t pt-4 text-sm text-gray-500">{d.assignedCount} viajes asignados</div></article>)}</div></div>; }
